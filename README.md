# NESC1205-Lab-09

🔐 SecureAuth: FastAPI & Modern Frontend
ระบบยืนยันตัวตนความปลอดภัยสูงที่พัฒนาด้วย FastAPI (Backend) และ Vanilla JavaScript (Frontend) โดยเน้นความลื่นไหลของ User Experience และความปลอดภัยของข้อมูล

🚀 ฟีเจอร์หลัก (Key Features)
ระบบ Login/Register: สมัครสมาชิกและเข้าสู่ระบบด้วย Email และ Password

Token Rotation System: จัดการ Access Token และ Refresh Token โดยอัตโนมัติผ่าน apiCall()

Profile Management: แสดงข้อมูลผู้ใช้ที่ดึงมาจาก API แบบ Real-time

Security Tools:

JWT Decoder: ตรวจสอบข้อมูล Payload ภายใน Token ได้จากหน้าเว็บ

Bcrypt Demo: จำลองการแฮชรหัสผ่านเพื่อความปลอดภัย

Health Check: ระบบตรวจสอบสถานะการเชื่อมต่อกับ API Server

Modern UI: ดีไซน์แบบ Dark Mode ด้วย Glassmorphism CSS

📂 โครงสร้างโปรเจกต์ (Project Structure)
Plaintext

fastapi_www/
├── index.html          # หน้าแรกและระบบตรวจสอบสถานะ Server
├── login.html          # หน้าเข้าสู่ระบบ
├── register.html       # หน้าสมัครสมาชิกพร้อม Password Requirements
├── profile.html        # หน้า Dashboard แสดงข้อมูลผู้ใช้
├── security.html       # เครื่องมือด้านความปลอดภัย (JWT/Bcrypt)
├── css/
│   └── style.css       # การตั้งค่า Theme และ Styles ทั้งหมด
└── js/
    ├── app.js          # Logic หลักของระบบ, การเรียก API และ Token Management
    └── auth.js         # ระบบตรวจสอบสถานะการ Login เบื้องต้น
🛠 วิธีการติดตั้งและรัน (Setup & Installation)
1. เตรียม Backend
ตรวจสอบให้แน่ใจว่าได้รัน FastAPI Backend อยู่ที่พอร์ต 8000 และเปิดใช้งาน CORS เรียบร้อยแล้ว

2. รัน Frontend
คุณสามารถเลือกใช้คำสั่งใดคำสั่งหนึ่งด้านล่างนี้ในโฟลเดอร์ fastapi_www:

Python: python -m http.server 3000

Node.js: npx serve -p 3000

PHP: php -S localhost:3000

เปิดเบราว์เซอร์ไปที่: http://localhost:3000

🔐 รายละเอียดด้านความปลอดภัย (Technical Security)
Token Management: เมื่อ Access Token หมดอายุ (401 Unauthorized) ระบบจะพยายามใช้ Refresh Token เพื่อขอ Token ใหม่ให้อัตโนมัติก่อนที่จะบังคับให้ผู้ใช้ Logout

Password Policy: หน้าสมัครสมาชิกมีการแจ้งเตือนเงื่อนไขรหัสผ่าน (8 ตัวอักษร, พิมพ์ใหญ่, อักขระพิเศษ)

Data Storage: เก็บ Tokens ใน localStorage เพื่อให้เซสชันคงอยู่เมื่อรีเฟรชหน้าเว็บ
